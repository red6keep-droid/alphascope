# 개발 구현 기록 현황 -1 (Alpha-Scope) — 트럼프 SNS 트렌드 리포트

이 문서는 [개발-구현-기록-현황.md](개발-구현-기록-현황.md)(2026-08-21까지)의 후속이다.
**2026-09-15 ~ 09-18에 진행한 트럼프 SNS 트렌드 리포트**의 현재 상태와, 새 세션에서 그대로 이어받기 위한 정보를 담는다.

- 저장소: `github.com/red6keep-droid/alphascope` · 브랜치 `main` (원격과 동기화됨, 마지막 커밋 `fb73522`)
- 코드: [`experiments/trump-trend/`](experiments/trump-trend/) · 기획서: [`doc/트럼프-SNS-트렌드-리포트-기획.md`](doc/트럼프-SNS-트렌드-리포트-기획.md) · 상세 README: [`experiments/trump-trend/README.md`](experiments/trump-trend/README.md)
- 최종 갱신: 2026-09-18

---

## 0. 새 세션에서 이어가는 법

1. 이 파일을 읽는다. 다음으로 `experiments/trump-trend/README.md`의 **검증 기록**과 **다음 단계** 절을 읽는다.
2. 현재 산출물은 GitHub `trump-state` 브랜치 → `experiments/trump-trend/output/state/trump_report.md`(또는 `.html`)에서 본다. 로컬 `output/`은 gitignore라 새 PC에는 없다.
3. 로컬에서 돌려 보려면 (리포 루트, `.env`에 `GEMINI_API_KEY`·`FRED_API_KEY` 필요):
   ```powershell
   pip install -r experiments/trump-trend/requirements.txt
   python experiments/trump-trend/main.py --skip-classify      # Gemini 없이 수집·집계·렌더까지 (약 15초)
   python experiments/trump-trend/main.py                      # 분류 포함 (첫 실행은 90일 백필 3회 필요)
   ```
   로컬 DB(`data/trump.db`)가 없으면 첫 실행이 archive 전체(약 3만 6천 건)를 받아 새로 만든다. 분류 결과는
   `trump-state` 브랜치의 `classifications.jsonl`을 `--import-state`로 붙이면 다시 Gemini를 부르지 않는다.
4. **지금 시점의 열린 결정 3개는 6절.** 코드 작업은 그 결정이 있어야 진행된다.

---

## 1. 이 프로젝트가 하는 것

Trump의 Truth Social 게시물을 매일 수집 → Gemini로 라벨링 → **이벤트 단위**로 묶기 → 3D/7D/30D/90D 트렌드와 Trend Score →
과거 유사 발언 이후 시장의 **SPY 대비 초과 반응** 통계 → 별도 글 시리즈로 게시.

핵심 원칙 (기획서와 코드 전체를 관통):
- **숫자는 파이썬, 문장은 Gemini.** Gemini는 게시물 라벨링(입력)과 집계 JSON 서술(출력) 두 자리만.
- **시장 영향은 AI가 판단하지 않는다.** 가격으로 계산한다 (`ai_market_impact` 컬럼 없음).
- **게시물 1개 ≠ 이벤트 1개.** 같은 topic+subtopic+target이 60분 이내면 하나의 `event_id`.
- **인과를 단정하지 않는다.** "반응이 관찰됐다" 어법. 서술 검증이 금지 표현을 걸러낸다.
- **노이즈는 버리지 않고 표시한다.** `noise_reason`(사전 필터) + `ai_market_relevance` 0–3.

---

## 2. 현재 상태 한눈에

| 항목 | 상태 |
| --- | --- |
| 파이프라인 코드 | **완료.** 수집·필터·분류·이벤트·일 단위 이벤트 스터디·집계·MD/HTML 렌더·서술·게시 |
| 90일 백필 | **완료.** 1,295건 분류, 검증 탈락·실패 0, 이벤트 206개 (2026-09-15 기준) |
| 수동 검증 (④) | **완료.** 50개 표본 일치율 94% (사전 필터 100%, Gemini 91%). `output/review_sample.md` |
| GitHub Actions | **가동 중.** `Trump Trend` 매일 22:30 UTC(07:30 KST). 09-16, 09-17 스케줄 실행 모두 성공 |
| 상태 저장 | `trump-state` 고아 브랜치. `classifications.jsonl`(약 410KB) + 리포트 4종. 매일 force push |
| 게시 | **꺼져 있음(그림자 모드).** 저장소 변수 `TRUMP_PUBLISH=true`로 켠다. 코드 변경 불필요 |
| 분봉(+5m/15m/60m) | **미구현.** 컬럼만 비어 있음. 무료 분봉 API 계정 결정 대기 |
| 테마 반영 | **보류.** 블로그 메인 개편 때 함께 (내비 라벨·목록 버튼) |

---

## 3. 구현 내역 (파일 기준)

```
experiments/trump-trend/
├── main.py             # 9단계 실행. --skip-* / --max-batches / --narrate / --publish / --refilter / --import-state / --export-state
├── config.py           # 확정값 전부. 숫자를 바꿀 때 여기만
├── db.py               # SQLite 스키마 5테이블 + 컬럼 마이그레이션
├── collect_posts.py    # CNN archive JSON(20MB, 2022~) → 새 id만 저장 + 사전 필터. --refilter 재적용
├── prefilter.py        # no_text / bare_reshare / self_repost / greeting / too_short
├── labels.py           # subtopic 표기 통일 (Tariffs → Tariff)
├── collect_prices.py   # yfinance 일봉 → daily_bars. 첫 실행 2y
├── calendar_macro.py   # FOMC(정적 2025–26) + FRED 릴리스(CPI·NFP·GDP·PCE) + 관찰 종목 실적일
├── gemini_client.py    # 다중 키 라운드로빈 · 429 키별 60s 쿨다운 · 503 지수 백오프
├── classify_posts.py   # 배치 15건 Gemini → 항목별 검증 → ai_* 컬럼. 최신 글부터
├── mapping_rules.json  # 섹터 → ETF → 상위 10종목 정적 규칙 13개 (rule_id · path_text)
├── mapping.py          # 첫 일치 규칙 적용
├── cluster_events.py   # event_id · 가중 다수결 방향 · 최대 강도 · 세션 · 기준일/측정일 · confounded 2종
├── event_study.py      # ret_close / ret_next_close · abn = 자산 − SPY
├── aggregate.py        # 창별 트렌드(같은 길이 비교) · Trend Score(가중합) · 반응 통계(관측일 합침) → trump_analysis.json
├── narrate.py          # Gemini 서술 → narrative.json. 금지 표현 검증
├── render_report.py    # → trump_report.md (점검용)
├── render_html.py      # → trump_report_body.html (Blogger 본문) + trump_report.html (미리보기)
├── report_title.py     # 제목 "트럼프 발언 트렌드 — YYYY년 M월 D일" · 라벨
├── publish.py          # Blogger 게시. daily-report의 publish_blogger 재사용. 같은 제목 있으면 건너뜀
├── state_io.py         # 분류 결과 JSONL 내보내기/불러오기 (Actions용)
├── review_sample.py    # 수동 검증 표본 50개 추출
├── prompts/classify.txt · prompts/narrate.txt
├── data/  (gitignore)  # trump.db · raw/posts/*.jsonl
└── output/ (gitignore) # 산출물 · state/
.github/workflows/trump-trend.yml
```

### 3.1 확정값 (config.py)

| 항목 | 값 |
| --- | --- |
| 클러스터 간격 | 60분 |
| 이벤트 방향 / 강도 | intensity 가중 다수결(동률 neutral) / 최대값 |
| relevance 임계값 | 트렌드 ≥ 1 · 이벤트 ≥ 2 |
| 창 | 3D/7D/30D/90D, **같은 길이끼리** 비교 |
| Trend Score 가중치 | 빈도 0.35 · 강도 0.30 · 최근성 0.25 · 신규성 0.10 (순위 백분위 가중합) |
| 통계 게시 조건 | clean 관측일 N ≥ 20 |
| 관찰 종목 (11) | SPY QQQ · SMH XLK XLF XLE XLV XLI · NVDA TSLA AAPL |
| 분류 | 배치 15건 × 실행당 최대 40배치, 최근 90일, `gemini-3.5-flash`, temperature 0 |

### 3.2 백필 중 발견해 고친 것 (기획서에 없던 결정)

- `Tariff`/`Tariffs` 분리 집계 → `labels.py` 정규화 (쓰기·읽기 양쪽)
- 자기 재게시 `RT @realDonaldTrump…`가 원문과 이중 계산 → `self_repost` 필터, 1,593행 재적용
- 같은 주제 이벤트가 같은 거래일에 여럿이면 N 과대 → (주제, 기준일, 측정일) 단위로 합침
- Gemini 503(수요 폭주)이 키 실패로 처리됨 → 일시 오류는 키를 쉬게 하지 않고 지수 백오프
- 서술이 전체 미분류(범위 밖 17,904건)를 인용 → 90일 범위 안 잔여만 인용하도록 프롬프트·상태 표시 수정
- daily-report 폴더의 같은 이름 모듈(`render_html`, `report_title`)이 이쪽을 가림 → `sys.path.append`로 순서 조정

### 3.3 기획서와 다른 점

- 원본 스냅샷은 Parquet 대신 **JSONL** (pyarrow 의존 회피)
- 분봉 미구현 (9절 판단: MVP는 일 단위로 성립. 무료 백필 API 확인 후)
- 실적 발표일은 yfinance `earnings_dates`, 관찰 종목 3개만

---

## 4. 운영 정보

### 4.1 GitHub Actions — `Trump Trend`

- cron `30 22 * * *` (매일, 주말 포함). 수동 실행 입력: `max_batches`(40) · `narrate`(true) · `publish`(false)
- 게시 게이트: 스케줄은 `vars.TRUMP_PUBLISH == 'true'`일 때만, 수동은 `publish` 입력
- 시크릿: 기존 `GEMINI_API_KEY`(`;` 구분 다중 키) · `FRED_API_KEY` · `BLOGGER_CLIENT_ID/SECRET/REFRESH_TOKEN` 재사용
- 흐름: `trump-state` fetch → `main.py --import-state --export-state --narrate [--publish]` → 상태+리포트 force push → 아티팩트(14일)
- 분류 결과는 분류 직후 내보내므로 뒤 단계 실패에도 Gemini 호출이 낭비되지 않는다

### 4.2 데이터 소스와 한도

| 소스 | 경로 | 비고 |
| --- | --- | --- |
| 게시물 | `https://ix.cnn.io/data/truth-social/truth_archive.json` | 공개, 키 없음, 5분 갱신, 2022-02~ 전체. Truth Social 직접 접근 없음 |
| 일봉 | yfinance | 비공식. 기존 데일리 리포트와 동일 조건 |
| 거시 일정 | FRED `release/dates` (CPI 10 · NFP 50 · GDP 53 · PCE 54) | FOMC는 `config.FOMC_DECISION_DATES` 정적 |
| 분류·서술 | Gemini 무료 등급 | 한도 숫자는 코드에 박지 않음. 하루 40배치면 충분 |

### 4.3 법적·운영 판단 (2026-09-15 정리)

- 공개 아카이브를 하루 1회 받아 집계하는 개인 연구 구조 → 실질 위험 낮음
- 하지 말 것: CNN 파일 재배포, 원문 대량 인용, 투자 권고 표현, Truth Social 직접 스크래핑
- 저장소 공개/비공개: 트럼프 파이프라인은 어느 쪽이든 무관. 비공개 전환의 걸림돌은 데일리 리포트의 jsDelivr 이미지(공개 저장소 필요)
- Actions 무료 시간은 **계정 단위** — 저장소를 나눠도 늘지 않음

---

## 5. 커밋 이력 (이 작업분)

| 커밋 | 내용 |
| --- | --- |
| `6e76642` | 파이프라인 전체 + 기획서 (그림자 모드) |
| `cbc1dcc` | GitHub Actions 매일 실행 + `state_io` + `trump-state` 시드 브랜치 |
| `fb73522` | 별도 글 게시 경로(`render_html` · `publish` · `report_title`) + `TRUMP_PUBLISH` 게이트 |

작업 트리: `개발-구현-기록-현황.md`에 빈 줄 1개 변경(사용자 편집)만 있음.

---

## 6. 열린 결정 (사용자 판단 필요)

| # | 결정 | 기본 제안 | 코드 영향 |
| --- | --- | --- | --- |
| 1 | **게시 켜기 시점** | 그림자 모드 시작(09-16)에서 2주 → **09-30 전후** 저장소 변수 `TRUMP_PUBLISH=true` | 없음 |
| 2 | **분봉 API** | Polygon Basic 또는 Alpaca(IEX) 무료 계정 1개. 과거 분봉 백필 가능 여부·요청 한도 확인 | 있음 — `collect_bars` 신설, `event_reactions.ret_5m/15m/60m` 채우기, `confounded_intraday` 실사용 |
| 3 | **테마 배치** | 블로그 메인 개편 때 함께 (메모리에 기록됨) | `build_theme.py` — `NAV_LABELS`에 "트럼프 트렌드", 글 라벨별 "목록" 링크 |

---

## 7. 다음 작업 (데이터 축적 후)

| 시점 | 할 일 | 판단 기준 |
| --- | --- | --- |
| 09-30 전후 | ⑤ Trend Score 가중치·클러스터 간격 점검 | 7D 창에 주제당 이벤트가 3개 이상 쌓인 뒤. 순위가 직관과 맞는지 |
| 09-30 전후 | 추가 수동 검증 `review_sample.py --seed <다른 값>` | Company 주제 relevance 과대 경향(표본 1건) 확인 → 프롬프트 보강 여부 |
| 게시 첫 주 | 게시 글 확인 — 제목 중복 방지, 라벨, 표 렌더 | Blogger에서 본문 인라인 스타일이 깨지는 곳 없는지 |
| 결정 2 이후 | 분봉 결합 | 정규장 게시물 이벤트만 대상 |
| 2026년 말 | `config.FOMC_DECISION_DATES`에 2027 일정 추가 | 연준 공개 일정 |
| 분기마다 | `mapping_rules.json` ETF 상위 10종목 갱신 | 수동 |

---

## 8. 알아둘 함정

- `noise_reason`이 붙은 행은 분류·이벤트에서 빠진다. 사전 필터 규칙을 바꾸면 `--refilter`로 전체 재적용해야 기존 행에 반영된다.
- 이벤트·반응 테이블은 **매 실행 전부 재생성**된다. 규칙이 결정적이라 같은 입력 → 같은 `event_id`. 여기에 수동 수정을 넣으면 다음 실행에 사라진다.
- `trump-state`는 force push라 이력이 없다. 특정 날 리포트를 남기려면 아티팩트(14일)나 별도 보관이 필요하다.
- 로컬에서 `--publish`를 붙이면 **실제로 올라간다.** 미리보기는 `output/trump_report.html`.
- Actions 첫 실행이 상태 브랜치를 못 찾으면 90일 백필을 처음부터 한다 (하루 40배치 × 3일). 시드 브랜치가 있으니 정상 상황에서는 일어나지 않는다.
- 기존 `experiments/daily-report/`에 같은 이름의 모듈이 있다. `publish.py`가 그 폴더를 `sys.path` **뒤에** 붙이는 이유다. 앞에 넣으면 이쪽 `render_html`이 가려진다.
