# 개발 구현 기록 현황 (Alpha-Scope)

- 저장소: `github.com/red6keep-droid/alphascope`
- 블로그: `alpha-scope.blogspot.com` (알파스코프)
- 브랜치: `main` (원격 추적: origin/main)
- 최종 갱신: 2026-08-20

---

## 1. 프로젝트 개요

미국 증시 데일리 브리핑 블로그 운영 자동화. 데이터 수집 → AI 분석 → Blogger 게시를
일정 주기로 자동 수행하고, 블로그에 달린 댓글을 감지해 AI 답글 초안을 Slack 으로 전달하는
**반자동 댓글 시스템**까지 구현한 상태.

- 데이터 소스: FRED(거시), Yahoo Finance(지수/종목), CNBC RSS(뉴스)
- AI: Gemini (`gemini-3.5-flash`), 다중 API 키 fallback 지원
- 게시: Blogger API v3 (OAuth 2.0 Client ID + Refresh Token, **사용자 계정 방식**)

---

## 2. 저장소 구조

```
D:\Alpha-Scope.blogspot
├── fetch_stocks.py            # 기존: 10종목 15분봉 → data.json (gh-pages 용)
├── fetch_news.py              # 기존: CNBC RSS → news.json
├── blogger.html / index.html  # 기존: 블로그/랜딩 테마(Contempo)
├── news_test.html             # 기존: 뉴스 피드 테스트
├── .gitignore                 # .env, client_secret, output/ 등 커밋 차단
├── .github/workflows/
│   ├── update.yml             # 기존: 주식 데이터 15분 주기
│   ├── news.yml               # 기존: 뉴스 데이터 30분 주기
│   ├── test-daily-report.yml  # 데일리 리포트 (일정 dry-run + 수동 publish)
│   └── comment-reply.yml      # 댓글 감지 (5분 주기 + 상태 브랜치)
└── experiments/
    ├── daily-report/          # 데일리 리포트 자동 게시 파이프라인
    └── comment-reply/         # 댓글 반자동 답글 파이프라인
```

---

## 3. 구현 완료 내역

### 3.1 데일리 리포트 (experiments/daily-report) — 구현 완료 · 실게시 성공

**목적**: 매일 미국 증시 데일리 브리핑(한국어)을 만들어 Blogger 에 자동 게시.

**동작 흐름**
```
collect_fred + collect_yahoo + collect_news
→ report_input.json
→ generate_report (Gemini) → report.out.json
→ validate_report (실패 시 중단)
→ render_html → output/{report.html, report_body.html}
→ publish_blogger (--publish 시 실제 게시)
```

**설계 원칙**
- 숫자는 Python만 렌더링 / Gemini는 한국어 분석 문구 + 뉴스 index 선택만 → 환각 차단
- 검증 통과 시에만 게시 (`exit(1)` 패턴)
- 기본 dry-run, `--publish` 또는 env `PUBLISH_BLOG=true` 시 게시

**파일 구성**
| 파일 | 역할 |
|---|---|
| `main.py` | 파이프라인 오케스트레이터 (1/5~5/5 단계 출력) |
| `collect_fred.py` | FRED 실업률(UNRATE)/CPI(CPIAUCSL)/VIX(VIXCLS) |
| `collect_yahoo.py` | 지수 5종(^GSPC ^IXIC ^DJI ^RUT ^VIX) + 급등주/관심종목 TOP5 |
| `collect_news.py` | CNBC RSS 20건 |
| `generate_report.py` | Gemini 호출, 다중 키 fallback |
| `validate_report.py` | 구조/수치/뉴스 index 검증 |
| `render_html.py` | 원본 숫자 + Gemini 문구 → HTML |
| `publish_blogger.py` | Blogger 게시 (OAuth refresh token) |
| `setup_oauth.py` | 1회 브라우저/URL+코드 승인 → refresh token 발급 |
| `verify_blogger.py` | 게시 없이 인증/블로그 연결 확인 |
| `prompts/daily_report.txt` | Gemini 지시문 |
| `templates/report.html` | 리포트 HTML 템플릿 |

**보안/인증 전환: 서비스 계정 → OAuth Client ID**
- 초기: 서비스 계정(service account) 방식으로 작성 → 서비스 계정은 초대 수락 불가라
  Blogger 에서 신뢰성 없음이 확인되어 폐기
- 전환: **OAuth 2.0 Client ID(데스크톱 앱) + Refresh Token** 방식 채택
  - `setup_oauth.py` 가 URL + 코드 수동 2단계(원격/헤드리스 호환)로 토큰 발급
  - consent screen 을 Production 으로 전환해야 7일 만료 함정 해결

**주요 이슈 해결**
| 이슈 | 해결 |
|---|---|
| Gemini 응답 뒤 잡음으로 JSON 파싱 실패(`Extra data`) | 첫 `{`~마지막 `}` 구간만 파싱 |
| `except` 밖에서 `e` 참조 → `UnboundLocalError` | 마지막 오류를 `last_error` 로 저장 |
| Windows 콘솔 cp949 인코딩 깨짐 | `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` |
| GEMINI_API_KEY 5개 키 지원 | `;`/`,` 파싱 + 키당 2회 재시도, 실패 시 다음 키 |

**실게시 결과**
- blogId: `2170661412485304541`
- 게시글: `https://alpha-scope.blogspot.com/2026/08/2026-8-20.html` (HTTP 200)
- 전체 파이프라인(수집→Gemini→검증→렌더→게시) 통과

### 3.2 댓글 반자동 답글 (experiments/comment-reply) — 구현 완료 · 로컬 검증 통과

**목적**: 댓글 감지 → Gemini 답글 생성 → Slack 알림, 등록만 사람이 하는 반자동 흐름.
(공식 Blogger API 는 댓글 insert 를 지원하지 않으므로 알림까지 자동 후 신고 등록)

**동작 흐름**
```
comments.listByBlog (5분 주기 폴링)
→ 새 댓글 감지 (본인 댓글·중복·스팸 제외)
→ Gemini 답글 생성 (prompt 로 숫자/링크 지어내기 금지)
→ 답글 + 원글/관리자 링크를 Slack 으로 전송
→ 관리자가 링크에서 복사/붙여넣기 → 등록
```

**파일 구성**
| 파일 | 역할 |
|---|---|
| `main.py` | 파이프라인 실행 (`--baseline` / `--send` / `--test-*`) |
| `blogger_client.py` | Blogger API 댓글/게시글 조회 (OAuth refresh token) |
| `generate_reply.py` | Gemini 답글 생성 (다중 키 fallback, `{"reply": ...}` 파싱) |
| `notify_slack.py` | Slack 인커밍 웹훅 전송 (알림 채널: **Telegram → Slack 전환**) |
| `state.py` | 처리 완료 댓글 ID 추적 (`output/state.json`) |
| `prompts/comment_reply.txt` | 답글 작성 지침 + 스팸 판정(`"reply":"SPAM"`) |

**검증 결과**
- baseline: 기존 댓글 1건(본인 작성 "좋은 정보네요.") 처리 등록, 알림 없음 확인
- Gemini 답글 생성 dry-run 성공 (키 1/5)
- Slack 테스트 발송 성공, 가상 댓글 → 답글 → Slack 발송 성공
- 중복 방지: 첫 실행 baseline 자동 시드, 이후 상태 파일로 중복 알림 차단

---

## 4. GitHub Actions 워크플로우

| 워크플로우 | 일정 | 동작 |
|---|---|---|
| `update.yml` | 15분 | fetch_stocks → data.json (기존) |
| `news.yml` | 30분 | fetch_news → news.json (기존) |
| `test-daily-report.yml` | `0 21 * * 1-5` UTC(=KST 06:00) | 일정 실행 dry-run, 수동 dispatch `publish` 토글로 실게시 |
| `comment-reply.yml` | `*/5 * * * *` | 새 댓글 감지 → Slack 발송. 처리 이력은 별도 브랜치 `comment-state` 에 저장해 실행 간 유지 |

---

## 5. 환경변수 / Secrets

**로컬 `.env` (루트, gitignore 등록됨)**
- `FRED_API_KEY`, `GEMINI_API_KEY`(다중 키 `;`/`,` 구분), `GEMINI_MODEL`
- `BLOGGER_CLIENT_ID`, `BLOGGER_CLIENT_SECRET`, `BLOGGER_REFRESH_TOKEN` (setuptime 발급 완료)
- `SLACK_WEBHOOK_URL` (설정 완료)
- `BLOG_URL=http://alpha-scope.blogspot.com/`, `PUBLISH_BLOG=false`

**GitHub Secrets 필요 목록 (정가동 전 등록 필요)**
- `FRED_API_KEY`, `GEMINI_API_KEY`
- `BLOGGER_CLIENT_ID`, `BLOGGER_CLIENT_SECRET`, `BLOGGER_REFRESH_TOKEN`
- `SLACK_WEBHOOK_URL`

---

## 6. 보안 조치

- `client_secret.json` → 리포 밖 `%USERPROFILE%\.blogger-oauth\` 로 이동 (repo 바깥 보관)
- `.gitignore` 에 `client_secret*.json`, `**/client_secret*.json`, `.env`, `experiments/*/output/` 등록
- git 추적·이력 스캔 결과 시크릿 파일 이력 없음 확인 (커밋 이력 깨끗)
- OAuth refresh token 은 `.env`(로컬 전용) / GitHub Secret 으로만 관리, 코드에 하드코딩 금지
- 로그 출력 시 키 마스킹(`AIzaSyAL***`)

---

## 7. 커밋 이력

```
7b2a8b5 Add semi-automatic comment reply pipeline with Slack alert
c0f6810 Add daily report pipeline with Blogger OAuth publishing
4763ff0 Fix TradingView chart and add news feed pipeline
4ecd043 Change schedule to 15-minute interval
e95073c Fix jsDelivr purge to use GET (POST rejected)
d5f1e30 Rename workflow to reflect 10 tickers
8fcb577 Reduce to 10 tickers with 15m candles
0464085 Bump actions to Node 24 runtimes
5784f28 Add stock data pipeline
```

---

## 8. 검증/실측 기준

- FRED: 실업률 4.1 / CPI 332.81 / VIX 15.84 (2026-08-18~19 기준)
- 지수 수집 5/5, 급등주·관심종목 5건, 뉴스 20건
- Gemini: gemini-3.5-flash, 키 1/5 정상
- Blogger: blogId `2170661412485304541`, 실게시 URL 확인, OAuth token 자동 갱신 정상
- Slack: 웹훅 발송 정상

---

## 9. 남은 작업 / 다음 단계

- [ ] GitHub Secrets 등록 (FRED/GEMINI/BLOGGER 3종/SLACK)
- [ ] `comment-reply` 수동 dispatch 실행으로 CI 발송 검증 (상태 브랜치 저장 확인)
- [ ] 데일리 리포트 일정"실게시" 전환 여부 결정 (현재 일정 dry-run)
- [ ] 의도적 실제 댓글 유도 테스트 → Slack 알림 → 답글 등록 흐름 확인
- [ ] `client_secret.json` 삭제 여부 (값은 이미 `.env` 추출됨, 즉시 삭제 가능)
- [ ] (선택) 민감 정보 보호를 위한 루트 `.env` 원격 푸시 이력 최종 점검